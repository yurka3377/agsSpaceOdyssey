
#include "driver.h"



/*

void setup() {
  Serial.begin(9600);
  //pinMode(PIN_KEY, INPUT);
}

void loop() {
  static int i=0;
  dacWrite(26, 0);
  if (i==0){
    i=0;
  }
  //Serial.println(digitalRead(PIN_KEY));
  delay(500);
}

void gpioR_Setup(){
  for (int i=0; mapPins[i].pinArd != 0 && mapPins[i].pinControl != 0; i++){
    pinMode(mapPins[i].pinControl, INPUT);
  }
}

void gpioW_Setup(){
  for (int i=0; mapPins[i].pinArd != 0 && mapPins[i].pinControl != 0; i++){
    pinMode(mapPins[i].pinControl, OUTPUT);
  }
}

void gpioW_Test(){
  for (int i=0; mapPins[i].pinArd != 0 && mapPins[i].pinControl != 0; i++){
    digitalWrite(mapPins[i].pinControl, HIGH);
    delay(1000);
    digitalWrite(mapPins[i].pinControl, LOW);
  }
}

*/